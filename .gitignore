.env
__pycache__/
*.pyc

# Dev container configuration should be committed
# .devcontainer/

storage/          # EXCLUDE: All documents, ChromaDB files, and logs
/postgres_data/    # EXCLUDE: The persistent Postgres data volume
/chroma_db/        # EXCLUDE: The vector store files (if stored outside /storage)